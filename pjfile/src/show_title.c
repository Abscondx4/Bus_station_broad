#include "basis.h"

void *show_title(){
    while (1)
    {

    while (main_flag == false)
    {
        
    }

    int font_size = 48;
    int red = 229;
    int green = 229;
	int blue = 229;
    int pic_posi_x = 0;
    int pic_posi_y = 0;
    int bm_size_x = 600;
    int bm_size_y = 60;
    sleep(1);
    while (main_flag == false)
    {
        
    }

    // show_font("从化客运站", posi_x, posi_y, bm_size_x, bm_size_y, font_size, red, green, blue);
    show_font_bmp("人民大道南", pic_posi_x, pic_posi_y, "station_icon.bmp", 50, 8, font_size, red, green, blue);
    while (main_flag == true)
    {
        
    }

    }
    
}